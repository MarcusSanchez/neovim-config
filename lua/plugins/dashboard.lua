-- Dashboard is disabled for now (see snacks.lua: dashboard.enabled = false).
-- Old config kept below — uncomment this block (and remove the enabled=false
-- override in snacks.lua) to bring it back.

-- return {
--   "folke/snacks.nvim",
--   opts = {
--     dashboard = {
--       preset = {
--         header = "Welcome to Costco. I love you.",
--       },
--       sections = {
--         { section = "header", padding = 3 },
--         { section = "keys", gap = 1, padding = 1 },
--         { section = "startup" },
--       },
--     },
--   },
-- }

return {}
